{
  "name": "writingtool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./styles.css -o ./dist/styles.css --minify",
    "build": "npm run build:css && mkdir -p dist && cp index.html dist/ && cp app.js dist/ && cp -r fonts dist/ && cp -r public/images dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.7"
  },
  "dependencies": {
    "@tailwindcss/line-clamp": "^0.4.4"
  }
}
